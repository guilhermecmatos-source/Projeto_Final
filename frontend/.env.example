# Proxy via Next (next.config.js) → backend em http://127.0.0.1:3001
NEXT_PUBLIC_API_URL=/api

# Opcional: URL direta (sem proxy), se o front sempre rodar na porta 3000:
# NEXT_PUBLIC_API_URL=http://localhost:3001/api
